# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to source control (it contains secrets).
# The server will auto-load these variables when you run: python api/ask.py

# GitHub Models API Token (required)
# Get from: https://github.com/settings/tokens
GITHUB_TOKEN=example_token

# Google Sheets CSV Export URL (optional, but recommended)
# Publish your sheet as CSV, then copy the export URL
# Example: https://docs.google.com/spreadsheets/d/<SHEET_ID>/export?format=csv&gid=<SHEET_GID>
SHEET_CSV_URL=example_url

# GitHub Model (optional, defaults to openai/gpt-4o)
# Other options: github/gpt-4-turbo, meta-llama/Llama-2-7b, etc.
GITHUB_MODEL=openai/gpt-4o
